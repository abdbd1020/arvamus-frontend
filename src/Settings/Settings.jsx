import React from 'react';
import Sidebar from '../Sidebar';

const Settings = () => {
    return (
        <Sidebar>
            <div>
                <h1>Settings page</h1>
            </div>
        </Sidebar>
    );
};

export default Settings;